<div class="span10">
  <h1> <%= @news.title %> </h1>
  <%= image_tag @news.picture_url, class: 'leftimg news_img' %>

  <p> <%= @news.html.html_safe %></p>
    <p class="text-muted"> <%= @news.created_at %></p>

  <% if can? :destroy, News %>
</br> <%= link_to 'Видалити', @news, :confirm => 'Are you sure?',
          :method => :delete %>
  <% end %>
</div>
