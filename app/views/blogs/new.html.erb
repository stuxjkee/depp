<%= simple_form_for @blog do |f| %>
  <%= f.input :title, label: 'Заголовок' %>
  <%= f.input :html,label: 'Контент', as: :wysihtml5,:input_html => { :rows => 15 , :style => 'width: 100%'}%>
  <%= f.button :submit %>
<% end %>
